name: XY Malloc
on: [push, pull_request, release]
jobs:
  check_env:
    name: Check Environments
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: gcc --version
      - run: cmake --version
      - run: df -h
      - run: free -h
  run_test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps: 
      - run: |
          pwd
          mkdir build 
          ls -al
          cd build && ls -al && ls .. -al && cmake .. && make huge_page && ./huge_page

